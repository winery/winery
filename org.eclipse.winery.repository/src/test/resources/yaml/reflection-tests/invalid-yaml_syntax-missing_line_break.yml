tosca_definitions_version: tosca_simple_yaml_1_3 description: test whether parser can handle a missing line break

metadata:
  description: Invalid tosca_definitions_value because description is not separated with line break.
  targetNamespace: http://www.example.org/ns/simple/yaml/1.3/test
  tosca.version: 1.1
  exception: InvalidYamlSyntax
