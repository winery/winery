tosca_definitions_version: tosca_simple_yaml_1_3

description: >
  This is an example of a multi-line description using YAML. It permits line breaks for easier readability...
  
  if needed,. However, (multiple) line breaks are folded into a single space character when processed into a single string value
  
metadata:
  author: kleinech
