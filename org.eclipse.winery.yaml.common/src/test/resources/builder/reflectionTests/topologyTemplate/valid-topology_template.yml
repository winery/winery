tosca_definitions_version: tosca_simple_yaml_1_1

metadata:
  description: This test contains a valid topology template.
  targetNamespace: http://www.example.org/ns/simple/yaml/1.1/test
  tosca.version: 1.1
  reference: 3.8.1
  exception: None
  keyname: topology_template
  assert-typeof: |
    inputs.ipt1 = TParameterDefinition
    node_templates.ndt1 = TNodeTemplate
    relationship_templates.rltp1 = TRelationshipTemplate
    groups.gtd1 = TGroupDefinition
    policies.plc1 = TPolicyDefinition
    outputs.opt1 = TParameterDefinition
  assert: |
    description = Topology template description
    inputs.ipt1.type = integer
    node_templates.ndt1.type = ntp1
    relationship_templates.rltp1.type = tosca.relationships.Root
    groups.gtd1.type = tosca.groups.Root
    policies.plc1.type = tosca.policies.Update
    outputs.opt1.type = integer   

topology_template:
  description: Topology template description
  inputs: 
    ipt1: 
      type: integer     
  node_templates:
    ndt1:
      type: ntp1     
  relationship_templates:
    rltp1:
      type: tosca.relationships.Root
  groups:
    gtd1:
      type: tosca.groups.Root
  policies:
    plc1:
      type: tosca.policies.Update
  outputs:
    opt1: 
      type: integer
     
node_types:
  ntp1:
    description: Node type description
